/**
 * The theme file will house our styled-components theme variables
 * Our theme will be passed in via context in the ThemeProvider component inside App.js
 * We can then access these variables via props inside our styled component files
 */

const theme = {
  // color variables
  fernGreen: '#547c3c',
  everglade: '#244434',
  yellowGreen: '#bfd76b',
  cucumber: '#78b464',

  // we can add any valid css properties to this as we build out the app
};

export default theme;
